{
	"name": "@repo/db",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"db:generate": "prisma generate --no-engine",
		"db:push": "prisma db push --skip-generate",
		"test": "echo \"Error: no test specified\" && exit 1",
		"db:dev": "npx prisma migrate dev && npx prisma generate --no-engine",
		"db:seed": "prisma db seed"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"dependencies": {
		"@prisma/client": "^6.15.0"
	},
	"exports": {
		"./client": "./src/index.ts"
	},
	"devDependencies": {
		"prisma": "^6.15.0"
	}
}